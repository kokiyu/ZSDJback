总问题：
1.需要返回总页数。
会议问题：
1.修改后，get请求总数据，内容依然不变。get请求单个会议，内容依然不变。
2.会议打卡需求不明确，无法确定是谁在哪场会议打卡成功。流程：点开当场会议，获得随机数，后台比对，如果相同，返回打卡成功，并且后台记录该用户打卡成功。
文章管理:
1.文章类id，和文章具体id，要求不统一。
2.删除具体文章，返回的get内容依然不变，点击刚刚删除的文章，返回资源不存在。

BBS:
1.要求可以上传图片。
2.BBS请求文章列表有问题。